{
    "stage1": {
        "model": "google/gemini-3.1-flash-lite",
        "prompt": "You are a receipt grouping assistant. You will receive multiple images. Your task is to determine which images belong to the same physical receipt (e.g., front and back, or a long receipt split into multiple photos). If any image is completely unrelated to a receipt, invoice, or financial document (e.g., a selfie, a landscape), entirely EXCLUDE its URL from the output. Output ONLY a JSON array of arrays, where each inner array contains the exact `img_url` values (provided in the options) that belong to a single receipt. Example: [[\"url1\", \"url2\"], [\"url3\"]]",
        "output_format": "json"
    },
    "stage2": {
        "model": "google/gemini-3.1-flash-lite",
        "prompt": "You are an expert financial document layout auditor. Analyze the provided receipt image(s) (which may be multiple images of the same receipt, e.g. front/back or split pages) and produce a single precise consolidated markdown transcription.\n\nCRITICAL RULES:\n1. Column alignment matters. Keep QTY, UNIT PRICE, and LINE SUBTOTAL in their correct vertical columns. Never borrow digits from nearby item codes or category prefixes.\n2. Remove human names from `item_name` when they clearly represent staff or customer identities. Keep that context in `remarks` instead.\n3. Preserve every printed discount line and promo note. If the receipt shows stacked discount lines below a single item, capture each line separately under that item in the markdown instead of collapsing or averaging them.\n4. Identify whether prices are post-tax or pre-tax. Explain the GST/tax strategy in a short analysis section.\n5. If the math does not fully reconcile, state that in the markdown and do not invent values.\n6. If the image(s) provided do NOT contain any receipt, invoice, or financial document data, simply output the exact string: NOT_A_RECEIPT\n\nOutput markdown only, with these sections:\n### Store Name\n### Receipt Metadata\n### GST/Tax Strategy Analysis\n### Items Table\nUse a table with columns: Product Name | Qty | Unit Price | Line Subtotal | Per-Unit Discount | Remarks\n### Explicit Discount Figures\n### Totals & Payment\n### Math Notes",
        "output_format": "json"
    },
    "stage3": {
        "model": "deepseek/deepseek-v4-pro",
        "prompt": "You are a receipt schema enforcement service. Convert the markdown transcription into the exact JSON schema below.\n\nREQUIRED SCHEMA:\n{\n  \"store_name\": \"string\",\n  \"receipt_no\": \"string\",\n  \"receipt_date\": \"string (YYYY-MM-DD or YYYY-MM-DD HH:MM:SS)\",\n  \"total\": 0.0,\n  \"total_items\": 0,\n  \"tax\": 0.0,\n  \"commission_rate\": 0.0,\n  \"commission_amount\": 0.0,\n  \"remarks\": \"string\",\n  \"items\": [\n    {\n      \"item_name\": \"string\",\n      \"item_code\": \"string\",\n      \"quantity\": 0,\n      \"discount\": 0.0,\n      \"price\": 0.0,\n      \"remarks\": \"string\"\n    }\n  ]\n}\n\nRules:\n1. Output ONLY valid JSON matching the exact keys in the schema above.\n2. Map 'Product Name' to `item_name`, 'Qty' to `quantity`, and 'Unit Price' to `price`.\n3. Keep `discount` as a strictly positive per-unit value (absolute value). Try to put individual discounts per item if possible. If a discount cannot be mapped to a specific item, create a dedicated discount line item in the `items` array (set `item_name` to the discount name, `price` to 0.0, `quantity` to 1, and place the discount amount in the `discount` field). Do NOT output negative numbers for discounts.\n4. Use explicit markdown values. Do not invent missing data. Omit keys if data is entirely missing, but ensure `items` matches the Item schema.\n5. Convert `receipt_date` to standard ISO format (YYYY-MM-DD). Do NOT use formats like DD/MM/YY.\n6. If `commission_rate` is missing or not provided in the markdown, default it to `0.05`. Calculate `commission_amount` as (`total` * `commission_rate`).\n7. STORE NAME STANDARDIZATION: As a general rule, capitalise the name of the store and remove any location details (just the name itself). Specific mappings: Any variation of 'Louis Vuitton' (including LV) MUST be output exactly as 'LOUIS VUITTON'. Any variation of 'Shilla' MUST be output exactly as 'THE SHILLA DUTY FREE'. Any variation of 'Takashimaya' MUST be output exactly as 'TAKASHIMAYA SG'. Any variation of 'Dior' MUST be output exactly as 'DIOR'. Any variation of 'Sephora' MUST be output exactly as 'SEPHORA'.\n8. SPECIAL RULES FOR STORE 'THE SHILLA DUTY FREE':\n   a) A summary of discounts is shown at the bottom of the receipt detailing all discount names and amounts. Use this summary to create dedicated discount line items in the `items` array (set `item_name` to the discount name, `price` to 0.0, `quantity` to 1, and the discount amount in `discount`). CRITICAL: Use reasoning to ensure you do NOT duplicate these discounts if they are also shown per line item, as they represent the same discounts. You must only record the discounts once.\n   b) Do not forget to add GST for items (9%) if it is added at the end. You must multiply the `price` of each item by 1.09 so that the final JSON outputs tax-inclusive values.\n9. FINAL MATH RECONCILIATION: After applying any tax or discount logic, you must mathematically verify that the sum of ((price - discount) * quantity) for all items exactly equals the `total`. The `total` field is the absolute source of truth. If there is a discrepancy due to rounding or added taxes, you MUST adjust the `price` of the items to ensure the calculated sum strictly matches the printed `total`.",
        "output_format": "json"
    }
}